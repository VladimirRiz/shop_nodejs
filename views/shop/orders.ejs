<%- include('../includes/head.ejs') %>
    </head>
<body>
    <%- include('../includes/nav.ejs') %>
    <main class="main">
        <% if(orders.length <= 0) { %>
            Nothing yet!
        <% } else { %>
            <ul class="order__list">
                <% orders.forEach(order => {%>
                    <li class="order__list_item">
                        <h1 class="order__list_id"># <%= order._id %></h1>
                        <ul >
                                <% order.products.forEach(product => { %>
                                    <li ><%= product.title %> <%= product.quantity %> - <a href="/orders/<%= order._id %>"> Invoice</a></li> 
                               <% }) %>
                        </ul>
                    </li>
              <% }) %>
            </ul>
       <% } %>
    </main>
<%- include('../includes/end.ejs') %>